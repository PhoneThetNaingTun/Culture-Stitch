export const publicRoutes = [
  "/",
  "/home",
  "/categories",
  "/about-us",
  "/contact-us",
  "/cart",
];

export const authRoutes = ["/auth/login", "/auth/register", "/auth/error"];

export const apiAuthPrefix = ["/api/auth", "/api/customer/app"];

export const DEFAULT_LOGIN_REDIRECT = "/home";
