import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const test = createSlice({ name: "test", initialState, reducers: {} });

export const {} = test.actions;
export default test.reducer;
